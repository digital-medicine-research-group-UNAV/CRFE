[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.9,<3.14"

[pypi-dependencies]
crfe = ">=0.1.0"

[tasks]
check-crfe = "python -c \"from CRFE import CRFE, ParamParada, StoppingCriteria; print('CRFE OK')\""
